apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontapp
spec:
  rules:
  - host: application.cubix.localhost
    http:
      paths:
      - path: /dev
        pathType: Prefix
        backend:
          service:
            name: frontapp
            port:
              number: 80
