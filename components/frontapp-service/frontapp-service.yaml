apiVersion: v1
kind: Service
metadata:
  name: frontapp
  labels:
    app: frontapp
    training: block4
    optional: frontapp
spec:
  selector:
    app: frontapp
    training: block4
    optional: frontapp
  ports:
  - name: web
    port: 80
    targetPort: web
